// LeadStock — Tela de login

document.addEventListener('DOMContentLoaded', function () {

    // Ano atual no rodapé
    var yearEl = document.getElementById('year');
    if (yearEl) yearEl.textContent = new Date().getFullYear();

});